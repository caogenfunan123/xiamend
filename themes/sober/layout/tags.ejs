<h1 class="page-title">🏷️ 标签</h1>
<div class="tag-cloud">
  <% site.tags.each(function(tag){ %>
  <a class="tag-item" href="<%- url_for(tag.path) %>">#<%= tag.name %><span class="tag-count"><%= tag.length %></span></a>
  <% }) %>
</div>
